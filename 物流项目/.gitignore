# ==========================================================================
# 车辆智能调度 Agent —— 忽略规则
# ==========================================================================

# --------------------------------------------------------------------------
# ★ 敏感信息：绝不能提交
# --------------------------------------------------------------------------
# backend/.env 里有真实的 MySQL 密码与 JWT 密钥。
# 保留 .env.example 作为模板（它只有占位值），使用者复制后自行填写。
.env
.env.local
.env.*.local
*.pem
*.key
*.p12
credentials.json

# --------------------------------------------------------------------------
# Python
# --------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 虚拟环境（本项目用 conda 的 py312，此处只为防止误建）
.venv/
venv/
env/
ENV/

# 测试与类型检查缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# --------------------------------------------------------------------------
# 前端（frontend/.gitignore 里也有一份，这里做双保险）
# --------------------------------------------------------------------------
node_modules/
dist/
dist-ssr/
.vite/
*.local

# --------------------------------------------------------------------------
# 自检脚本的产物
# --------------------------------------------------------------------------
# 界面自检脚本会把截图写到这里：node scripts/verify-*.mjs
.verify/
# 兼容历史上路径拼接失误、散落在 frontend/ 根目录的截图
.verify*.png

# --------------------------------------------------------------------------
# 数据库与日志（本地运行时可能产生）
# --------------------------------------------------------------------------
*.sqlite
*.sqlite3
*.db
*.log
logs/

# --------------------------------------------------------------------------
# 编辑器 / 操作系统
# --------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ==========================================================================
# 说明：以下文件**需要**提交，请勿加入忽略
#   frontend/pnpm-lock.yaml   锁文件 —— 保证依赖版本可复现
#   backend/requirements.txt  依赖清单
#   backend/.env.example      配置模板（不含真实密钥）
# ==========================================================================
